from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Identifying shipping containers by automatically extracting the ID from images',
    author='Alexander',
    license='',
)
